hermes_sandboxed_agent:
  responses_api_agents:
    hermes_sandboxed_agent:
      entrypoint: app.py
      num_workers: 1
      resources_server:
        type: resources_servers
        name: ???
      model_server:
        type: responses_api_models
        name: policy_model
      model: ???  # Real model identity, used by Hermes's context and timeout logic.
      sandbox_provider: sandbox
      runtime_python: /opt/hermes/bin/python3
      concurrency: 4
      max_turns: 90
      sandbox_timeout: 10800
      api_timeout: 1800
      enabled_toolsets: [terminal, file]
      datasets: []
